import React from 'react';

const SearchMovies = () => (
  <div className="container" style={{ paddingTop: '40px', minHeight: '80vh' }}>
    <h1>Search Movies (Coming Soon)</h1>
  </div>
);

const BookingPage = () => (
  <div className="container" style={{ paddingTop: '40px', minHeight: '80vh' }}>
    <h1>Booking (Coming Soon)</h1>
  </div>
);

const SeatSelection = () => (
  <div className="container" style={{ paddingTop: '40px', minHeight: '80vh' }}>
    <h1>Seat Selection (Coming Soon)</h1>
  </div>
);

const PaymentPage = () => (
  <div className="container" style={{ paddingTop: '40px', minHeight: '80vh' }}>
    <h1>Payment (Coming Soon)</h1>
  </div>
);

const MyBookings = () => (
  <div className="container" style={{ paddingTop: '40px', minHeight: '80vh' }}>
    <h1>My Bookings (Coming Soon)</h1>
  </div>
);

const WatchMovie = () => (
  <div className="container" style={{ paddingTop: '40px', minHeight: '80vh' }}>
    <h1>Watch Movie (Coming Soon)</h1>
  </div>
);

const Profile = () => (
  <div className="container" style={{ paddingTop: '40px', minHeight: '80vh' }}>
    <h1>Profile (Coming Soon)</h1>
  </div>
);

const Recommendations = () => (
  <div className="container" style={{ paddingTop: '40px', minHeight: '80vh' }}>
    <h1>Recommendations (Coming Soon)</h1>
  </div>
);

export {
  SearchMovies,
  BookingPage,
  SeatSelection,
  PaymentPage,
  MyBookings,
  WatchMovie,
  Profile,
  Recommendations
};
